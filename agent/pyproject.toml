[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pymonitor-agent"
version = "26.7.11"
authors = [{ name = "박영문" }]
description = "Bundled read-only CPython inspection agent for PyMonitor."
requires-python = ">=3.10"
